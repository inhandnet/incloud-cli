pre-commit:
  commands:
    goimports:
      glob: "*.go"
      run: goimports -w -local github.com/inhandnet/incloud-cli {staged_files} && git add {staged_files}
      stage_fixed: true
    lint:
      glob: "*.go"
      run: golangci-lint run {staged_files}
